# 线性代数统一教材：几何主线与逐章审查

审查日期：2026-09-20。本文是同一套教材的编写计划，不建立另一版正文。审查对象为 `assets/linear.js`、`assets/lessons.js`、`assets/chapters/chapter-11.js` 至 `chapter-16.js`、`assets/books/linear-systems.js` 及其图形和检查脚本。本次文档审查不代表已完成全书数学审校。

## 一、目前为什么仍不像可以独立自学的教材

本轮扩写前的主要断层是：`linear-span` 只讲二维组合，`linear-map` 已把矩阵列解释为标准基的像，`linear-projection` 已推导直线投影和施密特过程，`linear-systems` 已展开消元、主元、秩和通解，但前三个仍按简短固定模板组织。现已将 `linear-span` 改为连续正文，详见末尾验收记录；矩阵变换、投影与六个专题的完整教学链仍需逐章深化。

扩写前，向量组的张成只在二维实验中出现，“基与维数”集中在一个定义框；本轮已经补上三维张成、基的唯一表示、维数交换证明和基坐标。其余主要断层仍是：矩阵乘法刚解释为变换复合，后面的相似公式却缺少完整的坐标变换推导；特征向量演示只取对角矩阵；二次型只展示等值曲线随系数变，没有展示同一条曲线为什么能通过换坐标去掉交叉项。

因此，继续增加一道计算题、一个孤立滑块，无法填平这些断层。需要让同一个对象沿着几何、代数和用途三种描述连续发展。

### 实际结构快照

以下为 2026-09-20 审查时的源文件统计。正文字符数是移除 HTML 标签和空白后的粗略长度，仍包含公式源码，不能据此推算正式印刷页数，也不作为完成度指标。

| 专题源文件 | 二级标题数 | 原专题 Canvas 数 | 原例题卡数 | 原折叠练习数 | 粗略正文字符数 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 第 11 章：行列式 | 11 | 2 | 11 | 8 | 6,920 |
| 第 12 章：矩阵 | 11 | 2 | 12 | 8 | 6,371 |
| 第 13 章：向量 | 10 | 1 | 10 | 8 | 6,128 |
| 第 14 章：方程组 | 7 | 1 | 10 | 8 | 4,805 |
| 第 15 章：特征值 | 8 | 1 | 10 | 8 | 5,376 |
| 第 16 章：二次型 | 6 | 1 | 10 | 8 | 3,559 |

第 14 章的基础理论现已转向 `linear-systems` 连续正文，不能将两个入口简单相加当成独立知识量。该连续正文有 12 节、7 个定义块、10 个带解释或证明的定理块、17 个完整例子块、4 个随文图，新增正文没有 exercise 块。其他专题目前没有达到相同的连续讲解深度。原六章均宣称“3 个交互演示”，与实际 Canvas 数不符，须随整理修正。

## 二、固定一条主线，避免散成动画合集

初学顺序应围绕一个问题逐步展开：**给定若干方向，怎样组合出向量；矩阵怎样改变这些方向；哪些信息被保留、哪些被压缩；怎样挑一组更合适的方向使问题变简单。**

| 教学阶段 | 贯穿问题 | 必须建立的公式与理由 | 需要对应的几何 |
| --- | --- | --- | --- |
| 向量与空间 | 哪些目标能被现有方向组合出来？ | 线性组合、张成、相关与无关；基的存在性和坐标唯一性；维数为何不取决于具体基 | 点、直线、平面、空间；第三支箭头离开已有平面才增加维数；固定向量在两组基下的坐标 |
| 矩阵与变换 | 为什么知道单位方向去了哪里，就知道任意向量去哪？ | 从线性性推出 `Ax=Σxᵢaᵢ`；从复合推出矩阵乘法；逆变换条件 | 输入与输出各自标明空间，基向量和整个网格同步变化；先 B 后 A 和先 A 后 B 的对照 |
| 面积、体积与秩 | 哪些独立方向被保留？还能倒回去吗？ | 二阶行列式来源；三阶体积；换列、数乘、倍加性质；`det(AB)=det A det B`；秩与可逆等价条件 | 有向面积和体积、翻转、压扁；面积不是负数，负号记录定向 |
| 方程组与解结构 | 哪些目标能达到？每个目标有多少种输入？ | 相容性、主元与自由变量、秩—零度、特解加零空间 | 输入约束的公共交集；输出列张成；零空间是不会改变输出的输入方向 |
| 内积与正交 | 怎样把一个方向拆成互不干扰的分量？ | 内积与夹角的关系、投影条件、最近点性质、施密特、正交矩阵保长度角度 | 正交分解、投影误差；三维平面的两步正交化；原基与标准正交基张成同一空间 |
| 特征值与相似 | 有没有经过变换后仍留在自己直线上的方向？ | 特征方程由齐次非零解导出；特征空间与重数；`AP=PΛ` 到对角化；相似是同一变换的不同坐标 | 固定的不变直线；错切只有一个特征方向，旋转可能没有实特征方向；在特征基下分方向伸缩 |
| 二次型 | 哪些方向让二次量增大、减小或保持零？ | 展开 `xᵀAx`；`x=Cy` 推出合同；正交主轴；惯性；顺序主子式与配方的联系 | 等值线和曲面的主轴旋转；正定碗面、负定倒碗、不定鞍面；零方向与退化 |

这是教学依赖，不要求新增七套平行入口。既有六章的知识索引和旧地址应保留，同一概念只指定一个完整主讲位置，其余位置链接回引。入门知识与后续深入主题应分别说明；最小二乘、SVD、Jordan 标准形等须在先修概念齐备后再系统展开。

## 三、逐章缺口：区分没有讲与讲得太短

| 位置 | 已有内容 | 仍需补齐的关键内容 |
| --- | --- | --- |
| `linear-span`、第 13 章前半 | 二维组合、张成、相关定义、一个唯一表示推导；原章有极大无关组与秩 | 三维“直线—平面—全空间”连续例；子空间的正例与反例；无关组的部分组、相关组的扩充组等性质及证明；基的存在与坐标唯一性；不同基为何同样多；选原矩阵主元列的理由；向量组等价就是张成相同 |
| 第 11 章 | 二元消元引出二阶式、面积声明、排列定义、性质、展开、克拉默 | 面积公式的推导；三阶体积而非只记六条对角线；一般性质与几何之间的联系；乘积行列式和逆的关系；展开为什么合法、代数余子式错行和为何为零；克拉默公式的多线性推导；面积/体积换算的完整应用 |
| `linear-map`、第 12 章 | 二维线性映射与列意义、复合顺序、逆与秩列表 | 长方形矩阵的真实输入输出模型；转置的具体操作与乘积转置的推导；三种初等矩阵与乘法的对应；为什么 `[A|I]→[I|A⁻¹]`；分块矩阵与维数；矩阵方程的左右顺序；逆的唯一性和乘积逆；秩定义的几个等价版本及一般秩不等式的理由 |
| `linear-projection`、第 13 章后半 | 从残差垂直推出直线投影，最近点证明，施密特公式 | 从余弦定理建立内积几何；柯西不等式与角度公式条件；三个向量的完整正交化算例；投影到平面的真实三维图；正交矩阵的列、行、逆和保距离条件；投影与坐标的区别 |
| `linear-systems`、第 14 章 | 本轮已展开等价行操作、主元、自由变量、秩判据、基础解系、参数分支、配液和流量应用 | 与先前向量/矩阵完整主讲位置统一；常见矩阵方程 `AX=B`、`XA=B` 的列/行分组应用；确保读者遇到秩、基、维数时已有足够先修。不能用本章的充分展开代替其余五章 |
| 第 15 章 | 特征方程、性质列表、重根判据、部分二阶算例 | 特征空间包含零、特征向量不含零的区别；代数重数和几何重数；矩阵与坐标改变的区分；相似公式逐步推导；相同特征值未必相似；从 `AP=PΛ` 证明可对角化充要条件；对称矩阵不同特征空间正交的证明；实特征值存在性和谱定理的合适层次说明；完整矩阵幂及状态迭代应用 |
| 第 16 章 | 对称表示、正交变换公式、惯性声明、正定判据、二阶配方 | 为什么交叉项分半；一般可逆换元与正交换元的不同作用；没有平方项时如何先制造平方项；从系数求出变换矩阵而非只报标准形；惯性为什么不变；正定/半正定/不定及不等价条件；二阶主子式条件从配方推出；主轴、能量或局部极值的完整使用过程 |

每个重要公式至少给出一例“从情境识别公式”、一例“条件变化后怎么处理”、一例“如何核验结果”；不是要求每个公式机械堆三道题。例子应写出为何选法、每步等价性和结果含义。形式相近的公式可在一个贯穿例中比较。

## 四、最优先落地的工作包：向量—张成—基—坐标

优先扩写既有 `linear-span` 为连续正文，不新增另一套“几何版”。本章是所有后续公式的共同先修，补它比继续扩充方程组例子更能改善整体可读性。

1. 先区分点、自由向量、坐标和基。用移动相同箭头说明向量不依赖起点，用同一个向量的两套坐标说明坐标依赖所选方向。
2. 从逐坐标运算推导首尾相接、数乘和线性组合。用位移或资源配方建立输入系数的实际意义，说明向量维数也可以表示多个指标，不只表示空间轴。
3. 系统讨论零个有效方向、一条直线、一个平面和全空间；同一三维实验固定两个不共线向量，改变第三向量的离面高度，并显示目标是否能达到。
4. 给出张成和相关定义；证明相关等价于至少一个向量可被其他向量表示，解释“三个相关”不代表“任意两个共线”。
5. 子空间至少包含过原点直线、过原点平面、齐次解集三个正例，以及不经过原点直线、第一象限、单位圆等反例；分别检验加法或数乘哪一步失败。
6. 从“张成保证有表示、无关保证表示唯一”推出基；解释环境维数和子空间维数。零空间的维数为 0，不等于没有零向量。
7. 一步步解出同一向量的两套坐标；证明 `x=B[x]B`，把几何向量不动、坐标网格改变的图放在推导旁。基础阅读可把这一层作为理解相似的先修，一般基变换的完整理论再单独标深度。
8. 以一个平面子空间同时串起生成组、冗余向量、极大无关组、基、维数和坐标。章末说明下一章矩阵就是“记录各基方向的去向”，而不是突然转入一个新数表算法。

验收应核对：三维图对应实际向量和张成集合；有限窗口明确不等于集合边界；不同空间不混画；基退化时禁止除零并解释坐标不存在/不唯一；例子符号、所在空间和单位一致。正确性通过独立代数核验，视觉效果通过桌面和手机实测。

## 五、当前发现的数学与表述问题

下表保留本次最初审查的快照，便于说明问题来自哪里。2026-09-20 随后的修订已修正所列七项，具体实施和验证记录见表后；这不改变前文关于全书仍需扩写的判断。

| 位置 | 审查时的问题 | 应有的正确内容 |
| --- | --- | --- |
| 第 11 章三阶演示 `draw2` | 画的是 `[[a,1,1],[1,b,1],[1,1,c]]`，却把行列式算成 `abc` 并称该矩阵上三角。默认全 1 矩阵显示行列式 1 | 实际行列式为 `abc-a-b-c+2`，默认值 0；图形、矩阵、六项与读数必须使用同一模型 |
| 第 11 章面积演示 `draw1` | 向量按长度 `L=1.2` 画，读数使用 `sin(v-u)`；图中不说明是单位方向的归一化面积 | 若长度确为 1.2，面积应乘 `1.2²`；或改成明确的单位向量且用同一比例 |
| 第 13 章练习 5 | `(1,1,0),(0,1,1),(1,2,2)` 被答为秩 2，第三个等于前两个之和 | 前两者之和为 `(1,2,1)`，现有三个向量的行列式为 1、秩为 3 |
| 第 15 章特征方向演示 | 用 `abs(sin θ)<0.05` 或 `abs(cos θ)<0.05` 把接近轴的向量直接称为特征向量 | 对 `diag(2,1)`，精确实特征方向只有两轴。接近的方向可标“接近”，不能改变定义 |
| 第 15 章例 7 | 要求求 `A^10`，最终停在“计算 `PΛ^10P⁻¹`”，没有算出矩阵 | 对 `A=[[2,1],[1,2]]`，结果应完整给出 `[[29525,29524],[29524,29525]]` 并核验结构 |
| 第 15 章例 9 | 留有“`A=[[1,2],[2,1]]... 实际求 [[a,1],[1,a]]`”草稿残句 | 只保留统一清晰的含参矩阵和问题，推导对应其特征值 |
| 第 16 章例 10 | 证明写“正定 ⟺ 特征值全正 ⟺ 特征值积 >0”，最后的双向符号不成立 | 正定推出行列式为正；反例 `-I₂` 的行列式也为正而非正定。乘积只能给必要条件 |

### 同日修复记录

仅修订既有第 11、13、15、16 章，不删除章节、小节、Canvas 或折叠解析。

- 第 11 章面积读数改为从实际绘制向量分量计算有向面积，并标明向量长度 1.2。三阶演示保留非对角元素为 1 的满矩阵，画出原三列和辅助重复两列，完整画出六条 Sarrus 对角线，同时给出正三项、负三项和总结果；默认全 1 矩阵的读数为 0。原例 7 的未完成中间式也已清除；克拉默定理的第 i 行常数改为 `bᵢ`，并写清求和指标 j 与方程、未知数均为 n 的条件。
- 第 13 章原练习 5 保留题设，改为按列拼矩阵、两次消元、三个主元的完整过程，说明原组三向量就是极大无关组，并指出误答中的向量相加错误。
- 第 15 章按整数角滑块的精确轴方向判断特征向量，不再用接近阈值改变定义；底部解释移入画布内。例 7 乘完 `A^10`，并核验两个特征方向；例 9 明确统一的含参矩阵。
- 第 16 章补明正定只推出行列式正，以 `-I₂` 展示逆命题失败；移除开头“线代全书至此完结”的不实表述。四个修订章的交互数均改为实际数目，其余章的相同元数据问题留待相应章节整理。

独立回归 `checks/linear-corrections.cjs` 实际挂载旧章 Canvas：从绘制的单元数值重建矩阵，用独立高斯消元计算行列式，并逐一检查六条彩线确实穿过对应的三个单元；用实际填充四边形的鞋带面积核对有向面积读数；遍历全部整数角度核对特征方向。文本算例用独立连续乘法核对 `A^10`、用行列式核对原组三向量无关，并验证负定反例。运行结果为 1,331 个三阶图形状态、8 个面积状态和 181 个特征方向状态均通过。

四个修改文件语法检查通过。`checks/preservation.cjs` 确认全项目 17 个专题仍保留 170 个二级标题、44 个 Canvas、139 个折叠解析。此修复记录不把随后进行的 `linear-span` 连续正文扩写提前记为已完成；该部分仍需单独审阅其最终稿与新增几何模型。

## 六、本轮向量与基正文验收

`assets/books/linear-algebra.js` 现覆盖 13 个连续小节、19 个完整例子与应用、3 幅随文交互图。沿向量运算、线性组合、张成、相关与无关、子空间、基、维数、极大无关组、原列选基、基坐标展开。新增内容没有 exercise 或 activity 块，例子的推理直接展开。

本轮逐段审读了全部正文，重点核对：交换引理每步都能找到可替换项以及空组边界；存在表示与表示唯一分别由什么保证；原列和行化简后列空间的区别；方阵基的逆和矩形基的求解条件；平移平面与方向子空间；饲料配方从实数表示到非负可行区间。所有数值例子逐项代回，三维张成与基坐标模型另外通过独立消元和重构核验。

审查后补入列组合、列空间、行操作与主元的就地解释，原列关系证明只用可逆的等式操作，不要求先学逆矩阵；把“第一象限含零”改为准确的闭第一象限表述。特征值旧专题另将“只有一个特征向量”改为“只有一个线性无关的特征向量”，并说明基础解系的所有非零线性组合才是全部特征向量。

图解包含零子空间、直线、平面与三维空间四种模式；固定实际向量、改变基时，系数平面分别显示唯一坐标、无表示、无穷多表示。几何图使用有限参考窗口，正文明确它不代表张成的边界。坐标图保持各自等比例并标明不同视域，使用整数刻度。连续正文与所有六章原专题仍在同一阅读链，基础扩写不隐藏后续完整专题。

这次完成的是向量与基这一段主讲正文及已发现错误的修复；矩阵、一般向量组、特征值、二次型的逐公式推导和可视化仍按上述缺口继续编写，不能标记全书完成。

## 六、检查结果及其界限

审查时实际运行：

- `node checks/math.cjs`：75 个学习入口，346 个默认/边界渲染检查及已有数学恒等式检查通过。
- `node checks/linear.cjs`：259 个线代模型状态、8,398 个圆锥曲线点、先修链接和已有修正回归通过。
- `node checks/book.cjs`：319 个独立几何、导数、图形边界和阅读链接检查通过。

这些脚本通过仍未拦截第五节列出的错误。现有检查主要覆盖已抽取模型和结构，没有逐一核验旧 Canvas 代码的矩阵读数，更没有自动理解每段证明。后续应针对已经确认的错误增加独立计算回归；不能为了统计数量而写重复实现的测试，也不能把图能显示视为公式正确。

## 七、参考资料的使用边界

已有 [TEXTBOOK_REVIEW.md](TEXTBOOK_REVIEW.md) 记录了实际读取的 Interactive Linear Algebra 源文件和同济官方目录。几何讲解可以借鉴这些资料的组织次序，但每个定义、证明和模型仍需独立审校。

本次补充检索实际读到以下仓库 README 与目录；下列范围小于通读全书，引用时不得夸大：

- [Linear Algebra From the Ground Up](https://github.com/fiveapplesonthetable/linear-algebra-from-the-ground-up)：有实际 LaTeX 正文和编译 PDF，目录依次覆盖向量、张成、内积、矩阵、方程组、子空间、基、四子空间、正交、行列式、特征值等。README 的页数和图数是作者自述，本次未据此认证内容质量。
- [A First Course in Linear Algebra](https://github.com/rbeezer/fcla)：README 确认是公开完整线性代数入门教材并提供正式阅读入口；具体章节还需进一步读取，不能仅凭“完整”二字拿来验收本站。
- [Linear Algebra · Learn](https://github.com/gmcycle7/linear-algebra-learn)：README 明确已建成四章，其余多个基础主题仍在计划中；有几何交互启发，但不应称作已完整覆盖线性代数的教材。

GitHub 项目公开、图多或篇幅长，都不自动说明教学正确。对本项目最有用的是可核对的论证链、图与公式的精确对应，以及可以完整复算的应用例子。
